
const NAME = "v6.exchangerate-api"
const DOMAIN = `https://${NAME}.com`
const SITE =`${DOMAIN}/v6/`
const API_WP =`${SITE}688cb847a30ec45e77e33a63`
const GET =`${API_WP}/latest/`


export default {
    NAME,
    DOMAIN,
    SITE,
    API_WP,
    GET    
}